{
  "title": "OSK Sense",
  "config": {
    "step": {
      "user": {
        "title": "Connect to OSK Sense Hub",
        "description": "Create a read-only API token in the gateway web interface, then enter the gateway host and token.",
        "data": {
          "host": "Host",
          "token": "API token"
        },
        "data_description": {
          "host": "DNS name, IP address, or HTTP URL of the gateway.",
          "token": "Bearer token with the telemetry:read scope."
        }
      },
      "reauth_confirm": {
        "title": "Update OSK Sense authentication",
        "description": "Create a replacement read-only API token in the gateway web interface.",
        "data": {
          "token": "New API token"
        },
        "data_description": {
          "token": "Bearer token with the telemetry:read scope."
        }
      }
    },
    "error": {
      "cannot_connect": "Failed to connect to the gateway.",
      "invalid_auth": "The API token is invalid.",
      "invalid_host": "Enter a valid HTTP gateway host without a path.",
      "invalid_response": "The gateway returned an invalid response.",
      "unknown": "An unexpected error occurred.",
      "unsupported_version": "This gateway API or stream version is not supported."
    },
    "abort": {
      "already_configured": "This gateway is already configured.",
      "reauth_successful": "Authentication was updated successfully.",
      "wrong_gateway": "The new token belongs to a different gateway."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Configure a pulse counter",
        "description": "Choose the pulse-counter node whose pulses you want to convert to a physical total.",
        "data": { "device_uid": "Pulse counter" }
      },
      "counter": {
        "title": "Configure converted total",
        "description": "The converted total is calculated as pulse count × units per pulse. The raw pulse count remains available.",
        "data": {
          "units_per_pulse": "Units per pulse",
          "device_class": "Measurement type",
          "unit": "Unit"
        },
        "data_description": {
          "units_per_pulse": "Physical quantity represented by one pulse.",
          "unit": "Choose a Home Assistant unit or enter a custom unit when Measurement type is None."
        }
      }
    },
    "error": {
      "unit_required": "Enter a unit.",
      "incompatible_unit": "This unit is not compatible with the selected measurement type."
    },
    "abort": {
      "not_loaded": "Load the integration before changing pulse-counter options.",
      "no_pulse_counters": "No active pulse-counter nodes were found."
    }
  },
  "selector": {
    "pulse_device_class": {
      "options": {
        "water": "Water",
        "gas": "Gas",
        "energy": "Energy",
        "volume": "Volume",
        "none": "None / custom"
      }
    }
  }
}
